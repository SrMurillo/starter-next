# Starter (VS Code + Codespaces + Actions)

1) Abre Codespace: Code → Codespaces → Create codespace on main  
2) Crea proyecto (ej. React con Vite):
   npm create vite@latest my-app -- --template react
   cd my-app && npm i && npm run dev
